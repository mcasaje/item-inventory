package controllers.appusers.sessions;

import play.mvc.Http;

class SessionAuthControllerImpl implements SessionAuthController {

    @Override
    public void isSessionValid(Http.Session session) throws UnauthorizedException {
        // TODO
    }
}
