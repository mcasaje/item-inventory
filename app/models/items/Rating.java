package models.items;

/**
 * Immutable, a rating is a customizable numeric field used to sort {@link models.items.Item} objects or to give it a numerical attribute.
 */
public interface Rating extends Comparable<Rating> {
}
